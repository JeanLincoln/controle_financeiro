import { styled } from '..';

export const Container = styled('div', {
  display: 'grid',
  gridTemplateColumns: '1fr 1fr 1fr 1fr',
  placeItems: 'center',
  padding: '5rem 16rem 0',
  gap: '3.5rem',
});
